import 'package:flutter/material.dart';
import 'package:get/get.dart';

class AllColors {
  static Color primaryColor = Theme.of(Get.context!).colorScheme.primary;
  static Color whiteColor = Theme.of(Get.context!).colorScheme.onPrimary;
  static Color blackColor = Theme.of(Get.context!).colorScheme.shadow;
  static Color errorColor = Theme.of(Get.context!).colorScheme.error;
}
